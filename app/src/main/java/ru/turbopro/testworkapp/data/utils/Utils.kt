package ru.turbopro.testworkapp.data.utils

import java.util.*

enum class AddEditFruitErrors { NONE, ERR_ADD, ERR_ADD_IMG, ADDING }

enum class AddFruitObjectStatus { DONE, ERR_ADD, ADDING }

enum class StoreFruitDataStatus { LOADING, ERROR, DONE }